// Public GA4 measurement ID. Not a secret (it is visible in every page's
// network traffic once a visitor accepts analytics), so it lives in code
// rather than in a Vercel environment variable.
export const GA_MEASUREMENT_ID = "G-TCHJVNRX3W"
